# ==============================
# XOUND - Producción
# ==============================
# Las variables vienen del .env del servidor (cargado por Docker Compose)

spring.datasource.url=${SUPABASE_URL}
spring.datasource.username=${SUPABASE_USER:postgres}
spring.datasource.password=${SUPABASE_PASSWORD}

jwt.secret=${JWT_SECRET}

